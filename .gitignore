# 빌드 산출물
build/
dist/
release/
*.spec

# 가상환경
.venv/
.venv-build/
venv/

# 파이썬
__pycache__/
*.py[cod]

# 내 일정 (실제 데이터는 %APPDATA%\To-Do Manager 에 있다.
# 저장소에는 어떤 형태로도 들어가면 안 된다 - 팀에 그대로 공유된다)
data.json
data.json.*
*.log
selftest.txt

# 동기화 설정 (firebase/config.example.json 을 베껴 채운다). 저장소에 올리지 않는다.
firebase/config.local.json

# 휴대폰 앱 빌드 산출물 · 설정
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/firebase.local.properties
android/.kotlin/
