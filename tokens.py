# -*- coding: utf-8 -*-
"""디자인 값의 유일한 출처.

색과 굵기는 세 군데서 쓰인다.

    web/style.css   창 화면        (:root 블록을 여기서 만들어 넣는다)
    toast.py        알림 카드      (PIL 로 직접 그린다 - 여기서 바로 읽는다)
    tray.py         트레이 아이콘

예전에는 같은 색을 각자 적어 뒀다. 바탕을 한 단계 밝히려고 style.css 를
고치면 알림 카드만 예전 색으로 남아, 나란히 뜨면 종이 두 장의 색이 달랐다.
손으로 맞추는 한 언젠가 또 어긋난다.

이제 파이썬 쪽은 이 파일을 그대로 읽고, CSS 쪽은

    python tools/gen_tokens.py

가 :root 블록을 다시 써 넣는다. 어긋나면 테스트(test_tokens.py)가 잡는다.

나중에 다른 형태의 앱을 붙일 때도 사본을 하나 더 만들지 말고 여기서 읽는다.
"""

# ---------------- 색 ----------------
# 이 화면의 색은 두 층이다.
#
#   LIGHT   해 높이로 섞을 재료. 화면에서 실제로 보이는 바탕 · 면 · 글자색은
#           이 여덟 가지를 섞어서 그때그때 만든다 (web/app.js 의 applyLight).
#   COLOR   섞이지 않는 것들. 벽돌빛(지난 것) 처럼 뜻이 고정된 색과,
#           알림 카드 · 트레이 아이콘처럼 파이썬이 직접 그리는 곳의 값이다.
#
# 예전에는 COLOR 하나뿐이었다. 바탕이 고정이었으니 그걸로 충분했다.
# 이제 아침 · 낮 · 노을 · 밤이 다른 색이므로, 고정값으로 적어 두면 거짓이 된다.

# 해가 섞는 재료. 이름은 web/app.js 와 1:1 로 맞춘다.
LIGHT = {
    "base":     "#e4e9e3",   # 한낮의 바탕
    "panel":    "#eef2ec",   # 한낮의 면(종이)
    "ink":      "#1f2a28",   # 한낮의 글자
    "ink2":     "#5d6a67",   # 한낮의 보조 글자
    "teal":     "#3f6f69",   # 한낮의 강조
    "warm":     "#f4dcd4",   # 해 뜰 · 질 무렵의 땅 · 면 (참고 도안 '디오라마 하루' 의 노을 땅)
    "night":    "#1a2128",   # 해가 지고 섞어 넣는 밤
    "pale":     "#ffffff",   # 밤의 글자 (ink 가 여기로 간다)
    "pale2":    "#e2e7e4",   # 밤의 보조 글자
    "hol":      "#b0573f",   # 낮의 벽돌빛 (지난 것 · 공휴일 · 되돌릴 수 없는 것)
    "hol-lit":  "#f2ab97",   # 밤의 벽돌빛 - 어두운 면에서 #b0573f 는 3.3:1 뿐이다
    # 지난 일(시각이 지났거나 어제 것). 참고 도안의 호박빛이다. 예전에는 공휴일 ·
    # 삭제와 같은 벽돌빛이라 "놓친 것" 과 "되돌릴 수 없는 것" 이 같은 색으로 읽혔다.
    "late":     "#b1782f",
    "late-lit": "#e2b36f",   # 밤의 호박빛 - 어두운 면에서도 4.5:1 을 넘게
    # 되돌릴 수 없는 일 (삭제 · 완전히 종료 · 계정 지우기). 예전의 벽돌빛(#b0573f)은
    # 밤 면에서 바탕에 잠겨 읽히지 않았다. 낮에는 짙은 주홍, 밤에는 밝은 산호빛이다.
    "danger":     "#d4451a",
    "danger-lit": "#ff8a5c",
    # 궤도에서 지나온 쪽. 청록(teal)을 그대로 쓰면 낮 하늘 위에서 너무 무겁다 -
    # 낮에는 옅은 청록, 밤에는 남색 하늘에 가라앉은 청록이다.
    "rib-past":   "#7aa9a1",
    # 밤의 지나온 궤도. 회색(#415253)으로 두니 궤도가 색을 잃었다 - 청록 기운을 남긴다.
    "rib-past-n": "#467571",
    # 밤의 강조. 예전의 밝은 박하빛(#8fc2b9)은 매트한 밤 화면에서 혼자 떠 보였다 -
    # 초안의 가라앉은 청록이다. 채운 원 · 단추 위의 글자는 흰색이다 (--on-teal).
    "teal-lit": "#6fa199",
    # 하늘은 종이를 여러 겹 세운 디오라마다. 뒤로 갈수록 옅고, 앞으로 올수록
    # 짙으면서 그림자가 길어진다 - 그 깊이가 색으로도 한 번 더 말해진다.
    #
    # 겹마다 낮과 밤 두 값을 짝으로 둔다. 예전에는 낮 값 하나만 두고 밤에는
    # 전부 같은 밤빛(night)에 섞었는데, 그러면 여섯 겹이 밤에 한 가지 회색으로
    # 주저앉아 깊이가 사라졌다. 밤에도 뒤와 앞은 다른 색이어야 한다.
    "sky-hi":   "#b7d3e6",   # 하늘 꼭대기
    "sky-lo":   "#e2ecf0",   # 지평선 쪽 하늘
    "hill1":    "#dde9f0",   # 가장 먼 능선
    "hill2":    "#a8c4d2",
    "hill3":    "#7094a8",   # 가장 가까운 능선
    "ribbon":   "#e6ece8",   # 궤도 띠(아직 오지 않은 쪽)
    "rib-hi":   "#f7f9f6",   # 띠의 윗면 · 구슬 종이
    # ── 같은 여섯 겹의 밤. 하늘과 능선은 남색이다 (슬레이트 회색으로 바꿔 봤더니 밤
    #    하늘이 목록 판과 한 덩어리가 되었다). 목록 판 · 바탕은 그대로 슬레이트다 ──
    "sky-hi-n": "#141b24",
    "sky-lo-n": "#26313c",
    "hill1-n":  "#303e4c",
    "hill2-n":  "#1e2934",
    "hill3-n":  "#111820",
    "ribbon-n": "#3a4753",
    "rib-hi-n": "#4a5866",
    # ── 해 뜰 · 질 무렵 (참고 도안 '디오라마 하루'). 해가 지평선에 닿는 순간
    #    가장 짙고, 그로부터 한 시간쯤 멀어지면 거의 사라진다 ──
    "sky-hi-d": "#f2b98f",
    "sky-lo-d": "#f9d9d2",
    "hill1-d":  "#efb8b1",
    "hill2-d":  "#d3868f",
    "hill3-d":  "#94526a",
    "ribbon-d": "#f6d6cc",
    "rib-hi-d": "#fdeae4",
    "bead-d":   "#fdefe9",
    # 해는 그리지 않는다 (빛줄기로만 있다). 낮의 빛살은 흰 속 · 옅은 몸 · 따뜻한
    # 끝 세 겹이고, 노을에는 셋 다 분홍 쪽으로 기운다. 밤에는 달빛 두 색만 번진다.
    "ray-hot":    "#fffcf0",
    "ray-hot-d":  "#ffe2d2",
    "ray-pale":   "#fff6de",
    "ray-pale-d": "#fccdbe",
    "ray-warm":   "#f6c676",
    "ray-warm-d": "#f08c82",
    "ray-shade":  "#783c50",   # 빛 반대쪽 구석에 깔리는 그늘
    "moon":       "#e2ecf8",
    "moon-cool":  "#c4d6ec",
    # 게으름뱅이. 밤에도 어두운 덩어리로 남는다 - 글자색을 따라가면 밤에
    # 하얗게 뒤집혀 유령이 된다. 눈만 늘 밝다.
    "guy":      "#1f2a28",
    # 밤에는 흰 덩어리다 - 어두운 하늘 위에서 검은 덩어리는 구멍처럼 보였다.
    "guy-n":    "#c9cecc",
    "eye":      "#e4e9e3",   # 눈 흰자
    "eye-n":    "#8e9695",
    "pupil":    "#1f2a28",   # 눈동자
    "pupil-n":  "#2b3134",
}

# 섞이지 않는 색. 아래 이름들은 toast.py · tray.py · Theme.kt 가 그대로 읽는다.
COLOR = {
    # 바탕과 면 - LIGHT 의 한낮 값과 같다 (해가 없는 곳에서 쓰는 기본값)
    "bg":      "#e4e9e3",
    "card":    "#eef2ec",
    "dark":    "#c8d0cb",      # 눌린 그림자
    "light":   "#ffffff",      # 올라온 면의 빛
    "pale":    "#dbe3dd",      # 옅은 면 (배지 · 칩)
    "wash-hi": "#f1f4ef",
    "wash-lo": "#dbe1da",
    # 서류철은 버렸지만 휴대폰 쪽이 아직 쓴다
    "sheet2":  "#dfe4de",
    "sheet3":  "#d6dcd6",

    # 글자 (한낮 바탕 #e4e9e3 위 대비)
    "ink2":    "#1f2a28",      # 제목 · 시계   13.0:1
    "body":    "#2f3b38",      # 본문          10.2:1
    "muted":   "#46514e",      # 라벨           7.0:1
    "faint":   "#5d6a67",      # 보조           4.9:1
    "dim":     "#8a9491",      # 흐림(지난 것)  2.6:1 - 글자에는 쓰지 않는다

    # 강조
    "mint":    "#8fc2b9",
    "mid":     "#3f6f69",      # 강조 · 궤도 · 구슬  (5.2:1)
    "mid-ink": "#35605a",      # 청록을 글자로 쓸 때  (6.3:1)
    # 벽돌빛. 지난 것 · 공휴일 · 되돌릴 수 없는 동작이 모두 이 하나를 쓴다.
    # 한 화면에 빨강이 두 가지면 어느 쪽이 경고인지 흐려진다.
    "hol":     "#b0573f",
    "danger":  "#b0573f",
    "warm":    "#a9682f",      # 임박 (알림 카드에서만 쓴다)
    "deep":    "#16211f",
    "deep-hi": "#26322f",
    "onmid":   "#f2f6f3",      # 청록·먹 위에 얹는 글자
    # 입력칸 바탕. 면(card)보다 한 겹 밝다 - 여기가 '쓰는 곳' 이라는 표시.
    # 밤에는 자바스크립트가 반대로(면보다 살짝 밝게) 다시 만든다(--field).
    "field":   "#f6f8f6",
    "eye":     "#e4e9e3",      # 게으름뱅이의 눈 - 기본값 (app.js 가 해에 따라 --eye 를 다시 섞는다)
}

# 반투명 값은 색과 불투명도를 따로 둔다 (CSS 는 rgba, PIL 은 (r,g,b,a) 로 쓴다)
ALPHA = {
    # 실선 두 가지. 밤에는 자바스크립트가 밝은 쪽으로 뒤집는다(--hair / --hair2).
    "rule":       ("31,42,40", ".12"),    # 줄 사이 실선
    "rule2":      ("31,42,40", ".20"),    # 칸 제목 아래 실선
    # 마우스를 올렸을 때 덮는 색. 먹빛이 아니라 강조색을 옅게 깐다 -
    # 가리키는 줄과 궤도 위의 구슬이 같은 색으로 이어져야 한 쌍으로 읽힌다.
    "hover":      ("63,111,105", ".13"),  # 목록 줄 · 넓은 면
    "hover-hi":   ("63,111,105", ".20"),  # 단추 · 아이콘처럼 작은 것
    "next":       ("63,111,105", ".07"),  # 다음에 할 줄의 옅은 바탕
}


def rgba_tuple(name):
    """PIL 용 (r, g, b, a). CSS 의 rgba() 와 같은 값이 나온다."""
    rgb, a = ALPHA[name]
    r, g, b = (int(v) for v in rgb.split(","))
    return (r, g, b, int(round(float(a) * 255)))

# ---------------- 글자 굵기 ----------------
# Paperlogy 300 · 400 · 500 세 종.
# Bold(700) 는 한글에서 획이 서로 붙어 뭉툭해 보여 뺐다. 본문을 Medium 으로
# 내리고, 그만큼 보조 글씨는 Light 대신 Regular 로 올려 두 단계를 벌려 둔다.
# Light 는 큰 글자에만 남는다 - 42px 시계에서는 가는 획이 오히려 또렷하다.
WEIGHT = {
    "thin": 300,      # 큰 글자에만 (시계 · 창 제목 · 달력 연월)
    "sec":  400,      # 보조 · 라벨   (Light → Regular)
    "pri":  500,      # 본문 · 제목 · 숫자 (Bold → Medium)
}

# ---------------- 글자 스케일 ----------------
# 예전에는 서로 다른 크기가 열여덟 가지였다. 10.5 와 11 과 11.5 가 따로 있었는데
# 눈으로는 구별되지 않고, 고칠 때마다 "여긴 몇이었지" 를 다시 정하게 만들었다.
# 일곱 단이면 충분하다. 한 단 올리거나 내리는 것으로 끝난다.
#
# 자간은 크기에 딸려 다닌다. px 로 적으면 크기가 바뀔 때마다 또 어긋나므로 em 이다.
# 한글은 자소가 네모 칸을 꽉 채워서, 큰 글자는 좁혀야 덩어리로 읽히고
# 작은 글자는 벌려야 획이 산다.
TYPE = {
    # 자간은 단마다 한 번씩(0.01em) 벌렸다 - 창이 커지면서 글자가 서로 붙어 보였다.
    "display": ("44px",   "-0.02em"),   # 시계
    "title":   ("20px",   "-0.01em"),   # 달력 연월 · 큰 날짜
    "head":    ("15px",   "0em"),       # 창 제목
    "lead":    ("13px",   "0.01em"),    # 칸 제목 · 목록 줄
    "body":    ("12.5px", "0.015em"),   # 본문 · 입력칸 · 날짜
    "label":   ("11px",   "0.03em"),    # 시간대 이름 · 부제
    "micro":   ("10.5px", "0.04em"),    # 단추 라벨 · 눈금 · 배지
}

# ---------------- 여백 · 모서리 ----------------
# 2의 배수로만 센다. 1~3px 은 눈금이 아니라 광학 보정이라 여기 없다.
# 16 까지는 2px, 그 위는 4px, 더 위는 8px 씩. 조밀한 화면이라 4px 격자만으로는
# 한 단 차이가 너무 커서 줄이 성기어진다.
SPACE = ("2", "4", "6", "8", "10", "12", "14", "16", "20", "24", "28", "32", "34", "40", "48")
# 알약(높이의 절반)은 여기 없다 - 그건 999px 로 적는다. 높이를 바꿔도 따라온다.
RADIUS = {"xs": "4px", "sm": "8px", "md": "12px", "lg": "16px", "xl": "24px"}

# ---------------- 치수 · 움직임 ----------------
METRIC = {
    "rowpad": "12px",    # 목록 한 줄의 위아래 여백
    "tb":     "28px",    # 제목줄 높이 (참고 도안: 하늘 위에 얹힌 한 줄)
    # 자간. 한글은 자소가 네모 칸을 꽉 채워서, 0 보다 아주 조금 벌려야 글자가
    # 서로 붙지 않고 읽힌다. 큰 글자는 여기서 빼서 좁힌다 (calc(var(--track) - N)).
    "track":  ".1px",
}

# 움직임. 곡선은 할 일마다 하나씩이다 - 한 곡선으로 모두 움직이면 작은 것은 굼뜨고
# 큰 것은 툭 끊겨 보인다.
#   ease       나타나고 바뀌는 것 (빠르게 닿고 천천히 멈춘다)
#   ease-move  큰 면이 옮겨 가는 것 (하늘 접기 · 종이 · 화면 넘김). 출발이 부드럽고
#              끝에서 길게 가라앉는다
#   ease-exit  사라지는 것. 머뭇거리지 않고 빠진다 - 나타날 때보다 늘 짧다
#   ease-pop   작은 것이 톡 (완료 체크). 살짝 넘쳤다 돌아온다
MOTION = {
    "ease":      "cubic-bezier(.2,.8,.2,1)",
    "ease-move": "cubic-bezier(.32,.72,0,1)",
    "ease-exit": "cubic-bezier(.4,0,1,1)",
    "ease-pop":  "cubic-bezier(.34,1.56,.64,1)",
    "t-fast": "140ms",
    "t-med":  "220ms",
    "t-slow": "320ms",
    "t-move": "480ms",
}

# 돌멩이의 종이 소품 (web/pebble.js · style.css). 참고 도안 'LS 돌멩이 행동'.
PEBBLE = {
    "leaf":  "#9db98a",   # 낮잠 - 머리에 떨어진 나뭇잎
    "vein":  "#6f8f5e",   # 잎맥
    "sweat": "#bcd8e6",   # 헐떡일 때 땀방울
}

# 앱 아이콘 (tools/gen_icon.py). 참고 도안 'LazyScheduler 앱 아이콘' 확정안 8a -
# 저녁 하늘에 뜬 청록 달(체크), 겹겹의 언덕, 그 앞에 무게중심을 아래로 내린 흰 돌멩이.
ICON = {
    "sky":    "#6b7f8f",
    "moon":   "#6fa199",
    "check":  "#eef2ec",
    "hill1":  "#4e6171",   # 먼 언덕
    "hill2":  "#32404d",
    "ground": "#1e2730",
    "stone":  "#e2e7e5",
    "white":  "#f5f8f4",   # 눈 흰자
    "eye":    "#1e2730",
    "shadow": "#0c1216",
}


def rgba(name):
    """CSS 용 rgba() 문자열."""
    rgb, a = ALPHA[name]
    return "rgba(%s,%s)" % (rgb, a)


def css_root():
    """web/style.css 의 :root 블록 안에 들어갈 줄들.

    화면이 실제로 읽는 이름만 낸다. 예전의 책상 · 겹쳐 쌓인 종이 · 인덱스 탭 시절의
    이름(--desk · --sheet2 · --wash · --tab-off …)과 아무도 읽지 않는 여백 눈금 변수
    (--s8 …)는 뺐다 - 여백 눈금은 tests/test_design.py 가 SPACE 로 직접 지킨다.
    COLOR 의 나머지 색은 알림 카드(toast.py) · 트레이 · 휴대폰(Theme.kt)이 쓴다."""
    c = COLOR
    return "\n".join([
        "  --bg:%(bg)s;" % c,
        "  /* 해가 섞을 재료. web/app.js 가 여기서 읽어 --bg · --surface · --text 를 만든다. */",
        "  " + " ".join("--lit-%s:%s;" % (k, v) for k, v in LIGHT.items()),
        "  /* 한낮의 기본값. 자바스크립트가 창 요소에 덮어쓴다. */",
        "  --surface:%(panel)s; --text:%(ink)s; --text2:%(ink2)s; --teal:%(teal)s;" % LIGHT,
        # 청록을 채워 쓴 곳의 글자(--on-teal)와 먹빛 띠 위에 얹는 청록(--teal-inv)
        "  --on-teal:%(onmid)s; --teal-inv:%(mint)s; --field:%(field)s;" % c,
        ("  --sky-hi:%(sky-hi)s; --sky-lo:%(sky-lo)s; --hill1:%(hill1)s; "
         "--hill2:%(hill2)s; --hill3:%(hill3)s; --ribbon:%(ribbon)s; "
         "--rib-hi:%(rib-hi)s; --bead:%(rib-hi)s; --guy:%(guy)s; --late:%(late)s;") % LIGHT,
        "  --eye:%(eye)s;" % COLOR,
        "  --hair:%s; --hair2:%s; --next:%s;" % (rgba("rule"), rgba("rule2"), rgba("next")),
        "  --hover:%s; --hover-hi:%s;" % (rgba("hover"), rgba("hover-hi")),
        "  --hol:%(hol)s; --danger:%(danger)s;" % c,
        "  /* 굵기 단계. Light 는 작은 한글에서 획이 끊겨 읽히지 않아 큰 글자에만 남긴다. */",
        "  --w-thin:%(thin)d; --w-sec:%(sec)d; --w-pri:%(pri)d;" % WEIGHT,
        "  --rowpad:%(rowpad)s; --tb:%(tb)s;" % METRIC,
        "  /* 글자: 크기와 자간은 늘 짝으로 쓴다 (--fs-x 를 썼으면 --tr-x 도) */",
        "  " + " ".join("--fs-%s:%s; --tr-%s:%s;" % (k, v[0], k, v[1])
                        for k, v in TYPE.items()),
        "  /* 모서리 */",
        "  " + " ".join("--r-%s:%s;" % (k, v) for k, v in RADIUS.items()),
        "  --pb-leaf:%(leaf)s; --pb-vein:%(vein)s; --pb-sweat:%(sweat)s;" % PEBBLE,
        ("  --ease:%(ease)s; --ease-move:%(ease-move)s; --ease-exit:%(ease-exit)s; "
         "--ease-pop:%(ease-pop)s;") % MOTION,
        "  --t-fast:%(t-fast)s; --t-med:%(t-med)s; --t-slow:%(t-slow)s; --t-move:%(t-move)s;" % MOTION,
    ])
